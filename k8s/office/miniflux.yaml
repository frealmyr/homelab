apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: miniflux
  namespace: argocd
spec:
  destination:
    name: in-cluster
    namespace: home
  project: default
  source:
    targetRevision: main
    path: k8s/office/miniflux
    repoURL: https://github.com/frealmyr/homelab
    helm:
      releaseName: miniflux
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
