apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: credentials-cloudflare-tunnel
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
spec:
  secretStoreRef:
    name: homelab
    kind: ClusterSecretStore
  refreshInterval: "8760h" # Once each year, pretty static secrets. Will manually decrease secret when i need it to refresh.
  target:
    name: credentials-cloudflare-tunnel
    creationPolicy: Owner
    deletionPolicy: Retain
  data:
    - secretKey: token
      remoteRef:
        key: homelab-cloudflare
        property: tunnel_lychee
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: credentials-app
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
spec:
  secretStoreRef:
    name: homelab
    kind: ClusterSecretStore
  refreshInterval: "8760h" # Once each year, pretty static secrets. Will manually decrease secret when i need it to refresh.
  target:
    name: credentials-app
    creationPolicy: Owner
    deletionPolicy: Retain
  data:
    - secretKey: mapbox-token
      remoteRef:
        key: homelab-apps
        property: mapbox-photos-token
