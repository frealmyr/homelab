apiVersion: v2
name: storage
description: Storage, local-paths and nfs

type: application

version: 1.0.0

dependencies:
  - name: local-path-provisioner
    version: ">=0.0.0"
    repository: "https://charts.containeroo.ch"
