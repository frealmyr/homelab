apiVersion: v2
name: adguard
description: adblocker and dns server

type: application

version: 1.0.0

dependencies:
  - name: nginx
    version: ^11.0.0
    repository: "https://charts.bitnami.com/bitnami"
