apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: adguard
  namespace: argocd
spec:
  destination:
    name: in-cluster
    namespace: home
  project: default
  source:
    targetRevision: main
    path: k8s/home/adguard
    repoURL: https://github.com/frealmyr/homelab
    helm:
      releaseName: adguard
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
